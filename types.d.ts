export interface  User {
    id: string;
    name:string;
    email: string;
    category: string
    isActive: boolean;
}

export interface  DishMutation {
    name:string;
    email: string;
    category: string
    isActive: boolean;
}